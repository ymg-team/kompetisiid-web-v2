import Styled from "styled-components";
import { Colors } from "@configs/style";

export const EditorV2Styled = Styled.div`
.rsw-editor {
    border-radius: 0;
    border: none;
    border-bottom: 1.5px solid ${Colors.mainGray} !important;
}
`;
