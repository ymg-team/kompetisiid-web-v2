let showSearch = !1,
  showNavTop = !1;
function toggleNavTop() {
  (showNavTop = !showNavTop),
    (document.getElementById("top-menu").style.left = showNavTop
      ? "0"
      : "-50%");
}
function hasClass(e, t) {
  return e.classList
    ? e.classList.contains(t)
    : !!e.className.match(RegExp("(\\s|^)" + t + "(\\s|$)"));
}
function addClass(e, t) {
  e.classList ? e.classList.add(t) : hasClass(e, t) || (e.className += " " + t);
}
function removeClass(e, t) {
  if (e.classList) e.classList.remove(t);
  else if (hasClass(e, t)) {
    var s = RegExp("(\\s|^)" + t + "(\\s|$)");
    e.className = e.className.replace(s, " ");
  }
}
function modal(e, t) {
  let s = document.getElementById(t);
  "open" == e &&
    ((document.getElementsByTagName("body")[0].style.overflow = "hidden"),
    addClass(s, "open")),
    "close" == e &&
      ((document.getElementsByTagName("body")[0].style.overflow = "auto"),
      removeClass(s, "open"));
}
document.addEventListener("DOMContentLoaded", () => {
  let e = document.getElementById("btn-search");
  e &&
    (e.addEventListener("click", (e) => {
      toggleSearch();
    }),
    document
      .getElementById("btn-closesearch")
      .addEventListener("click", (e) => {
        toggleSearch();
      }),
    document.getElementById("btn-show-nav").addEventListener("click", (e) => {
      toggleNavTop();
    }),
    document.getElementById("btn-hide-nav").addEventListener("click", (e) => {
      toggleNavTop();
    })),
    (window.onclick = (e) => {
      if (
        e.target.matches(".btn.btn-white.btn-close-modal.btn-sm.fas.fa-times")
      ) {
        let { id: t } = e.target.parentNode.parentNode;
        t || (t = e.target.parentNode.parentNode.parentNode.id),
          modal("close", t);
      }
      let s = document.getElementsByClassName("dropdown-items");
      for (let a = 0; a < s.length; a++) {
        let n = s[a];
        n.classList.contains("show") && n.classList.remove("show");
      }
      let l = e.target.getAttribute("data-target");
      l && document.getElementById(l).classList.toggle("show");
    });
});
const registerServiceWorker = async () => {
    if (!("serviceWorker" in navigator)) return null;
    {
      let e = await navigator.serviceWorker.register("/sw.1.0.0.js", {
        scope: "/",
      });
      return e;
    }
  },
  main = async () => {
    await registerServiceWorker();
  };
main();
